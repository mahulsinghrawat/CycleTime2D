export const primaryButton = {
  width: "100%",
  padding: "12px 16px",
  borderRadius: 10,
  border: "none",
  background: "#2563eb",
  color: "white",
  fontWeight: 600,
  cursor: "pointer",
  transition: "0.2s",
  marginBottom: 10,
};

export const disabledButtonStyle = {
  background: "#d1d5db",
  color: "#6b7280",
  cursor: "not-allowed",
  opacity: 0.6,
};

export const secondaryButton = {
  width: "100%",
  padding: "12px 16px",
  borderRadius: 10,
  border: "1px solid #d1d5db",
  background: "white",
  color: "#111827",
  fontWeight: 600,
  cursor: "pointer",
  transition: "0.2s",
  marginBottom: 10,
};

// For destructive "Remove"/"Clear" actions — a light red tint instead of
// plain white so they read as distinct from neutral secondary actions.
export const dangerButton = {
  width: "100%",
  padding: "12px 16px",
  borderRadius: 10,
  border: "1px solid #fca5a5",
  background: "#fef2f2",
  color: "#b91c1c",
  fontWeight: 600,
  cursor: "pointer",
  transition: "0.2s",
  marginBottom: 10,
};

// Same green/purple used to mark LH/RH in the Motion Sequence table
// (Code(LH)/Code(RH) columns), reused so the toolbox's Left/Right hand
// buttons read as the same visual identity.
export const leftHandButton = {
  width: "100%",
  padding: "12px 16px",
  borderRadius: 10,
  border: "1px solid #86efac",
  background: "#dcfce7",
  color: "#166534",
  fontWeight: 600,
  cursor: "pointer",
  transition: "0.2s",
  marginBottom: 10,
};

export const rightHandButton = {
  width: "100%",
  padding: "12px 16px",
  borderRadius: 10,
  border: "1px solid #d8b4fe",
  background: "#f3e8ff",
  color: "#7e22ce",
  fontWeight: 600,
  cursor: "pointer",
  transition: "0.2s",
  marginBottom: 10,
};